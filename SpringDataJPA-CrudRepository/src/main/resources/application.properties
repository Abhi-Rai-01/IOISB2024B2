spring.application.name=SpringDataJPA-CrudRepository

spring.datasource.url=jdbc:mysql://localhost:3306/ioi_24b2_batch
spring.datasource.username=root
spring.datasource.password=23092004



# hibernate and JPA properties

spring.jpa.hibernate.ddl-auto=update
# The spring.jpa.hibernate.ddl-auto property controls how Hibernate manages the database 
# schema at startup. It supports values like none, validate, update, create, and create-drop, 
# each defining whether the schema is created, updated, validated, or dropped.
# It is typically set to update during development and validate or none in production.


spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true